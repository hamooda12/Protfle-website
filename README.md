# Hamad Tarawa Portfolio

This is the complete source for the portfolio website, prepared for GitHub Pages.

## Run locally

```bash
npm install
npm run dev
```

## Deploy with GitHub Pages

1. Create a new GitHub repository.
2. Upload all files from this folder to the repository root.
3. Open **Settings → Pages** in the repository.
4. Under **Build and deployment**, choose **GitHub Actions**.
5. Push to the `main` branch. The included workflow builds and deploys the site automatically.

The website uses relative production asset paths, so it works whether the repository is named `username.github.io` or anything else.
# Protfle-website
